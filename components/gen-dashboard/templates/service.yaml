apiVersion: v1
kind: Service
metadata:
  name: gen-dashboard
  labels:
    {{- include "gen-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gen-dashboard.selectorLabels" . | nindent 4 }}
